<div class="container">
	<div class="card">
		<div class="card-body bg-secondary">
			<%= form_with model: subject do |form|%>
			<%= form.text_field :name, placeholder: "Add Subject", class:"form-control mb-3" %>
			<%= form.submit class:"btn btn-primary" %>
			<% end %>
		</div>
	</div>
</div>